OPTIONS
default_rules yes
logging no

INTERFACES
lan     eth0    10.0.0.0/24
ext     eth1    0.0.0.0/0    wan

ALIASES
workstation     10.0.0.10

FIREWALL
lan [.] > ext

POLICIES
* / *

LIMITS
# Limit all egress traffic on the WAN to 100 Mbit/s.
on ext egress * > * 100mbit
# Limit traffic as it enters the router from the LAN.
on lan ingress * > * 100mbit
# Give the workstation an independent 10 Mbit/s upload ceiling.
on lan ingress workstation > * 10mbit
# Limit all traffic sent to the LAN and downloads to the workstation.
on lan egress * > * 100mbit
on lan egress * > workstation 25mbit

CUSTOM
